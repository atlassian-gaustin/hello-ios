target 'hello-ios' do
    platform :ios, '9.0'
    pod 'LaunchDarkly', :path => '../ios-client'
end
    
target 'hello-watchOS Extension' do
    platform :watchos, '2.0'
    pod 'LaunchDarkly', :path => '../ios-client'
end
